<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Config namespace
    |--------------------------------------------------------------------------
    |
    | This is the namespace that all site config items will be listed under.
    | For example, the default is 'site_config', so to access item 'foo'
    | you could use `config('site_config.foo')`.
    |
    */
    'config_namespace' => 'site_config',

    /*
    |--------------------------------------------------------------------------
    | Default layout view
    |--------------------------------------------------------------------------
    |
    | The blade view that will be used as the layout file for the default views.
    |
    */
    'layout' => 'site_config::layout',
];
